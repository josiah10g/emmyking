[build]
  command = "npm run build"
  publish = "dist/client"

# SPA fallback so client-side routes resolve on direct hits / refreshes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
